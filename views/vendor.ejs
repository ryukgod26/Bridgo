<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Vendor Dashboard</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: #f4f8fb;
            margin: 0;
            padding: 0;
        }
        .dashboard-container {
            max-width: 600px;
            margin: 40px auto;
            background: #fff;
            border-radius: 10px;
            box-shadow: 0 4px 24px rgba(46, 134, 222, 0.08);
            padding: 2.5rem 2rem 2rem 2rem;
            position: relative;
        }
        .dashboard-header {
            text-align: center;
            margin-bottom: 2rem;
            position: relative;
        }
        .dashboard-header h1 {
            color: #2e86de;
            font-size: 2.2rem;
            margin-bottom: 0.5rem;
        }
        .logout-btn {
            position: absolute;
            top: 0;
            right: 0;
            background: #e74c3c;
            color: #fff;
            border: none;
            border-radius: 6px;
            padding: 8px 18px;
            font-size: 1rem;
            font-weight: 500;
            cursor: pointer;
            transition: background 0.2s;
            text-decoration: none;
        }
        .logout-btn:hover {
            background: #c0392b;
        }
        .vendor-info {
            background: #eaf2fb;
            border-radius: 8px;
            padding: 1.2rem 1.5rem;
            margin-bottom: 2rem;
        }
        .vendor-info h2 {
            color: #2e86de;
            font-size: 1.3rem;
            margin-bottom: 0.7rem;
        }
        .vendor-info p {
            margin: 0.3rem 0;
            color: #333;
            font-size: 1rem;
        }
        .dashboard-actions {
            display: flex;
            justify-content: center;
            gap: 2rem;
        }
        .action-btn {
            background: #2e86de;
            color: #fff;
            border: none;
            border-radius: 6px;
            padding: 0.9rem 2.2rem;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            transition: background 0.2s;
            text-decoration: none;
            text-align: center;
            box-shadow: 0 2px 8px rgba(46, 134, 222, 0.08);
        }
        .action-btn:hover {
            background: #1b4f72;
        }
        @media (max-width: 600px) {
            .dashboard-container {
                max-width: 98vw;
                padding: 16px 6vw 18px 6vw;
            }
            .dashboard-header h1 {
                font-size: 1.4rem;
            }
            .dashboard-header p {
                font-size: 1rem;
            }
            .vendor-info {
                padding: 12px 8px;
                font-size: 0.98rem;
            }
            .dashboard-actions {
                flex-direction: column;
                gap: 14px;
                align-items: stretch;
            }
            .dashboard-actions a {
                width: 100%;
                font-size: 1rem;
                padding: 10px 0;
            }
            .logout-btn {
                padding: 7px 12px;
                font-size: 0.95rem;
            }
        }
    </style>
</head>
<body>
    <div class="dashboard-container">
        <div class="dashboard-header">
            <a href="/vendor/logout" class="logout-btn">Logout</a>
            <h1>Welcome, <%= vendor && vendor.name ? vendor.name : "Vendor" %>!</h1>
            <p>Your Vendor Dashboard</p>
        </div>
        <div class="vendor-info">
            <h2>Vendor Information</h2>
            <% if (vendor) { %>
                <p><strong>Vendor ID:</strong> <%= vendor.vendorId %></p>
                <p><strong>Name:</strong> <%= vendor.name %></p>
                <p><strong>Email:</strong> <%= vendor.email %></p>
                <p><strong>Phone:</strong> <%= vendor.phone %></p>
                <p><strong>GST:</strong> <%= vendor.gst %></p>
                <p><strong>Address:</strong>
                    <%= vendor.address %>
                    <% if (vendor.area) { %>, <%= vendor.area %><% } %>
                    <% if (vendor.city) { %>, <%= vendor.city %><% } %>
                    <% if (vendor.state) { %>, <%= vendor.state %><% } %>
                </p>
            <% } else { %>
                <p>Vendor information not available.</p>
            <% } %>
        </div>
        <div class="dashboard-actions">
            <a href="/vendor/auction/join" class="action-btn">Join Auction</a>
            <a href="/vendor/auction/create" class="action-btn">Create Auction</a>
        </div>
    </div>
</body>
</html>
