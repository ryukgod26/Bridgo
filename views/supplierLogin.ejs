<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Supplier Login</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: #f7f7f7;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        .login-container {
            background: #fff;
            padding: 2rem 2.5rem;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
            width: 350px;
        }
        .login-container h2 {
            text-align: center;
            margin-bottom: 1.5rem;
            color: #218838;
        }
        .form-group {
            margin-bottom: 1.2rem;
        }
        .form-group label {
            display: block;
            margin-bottom: 0.4rem;
            font-weight: 500;
            color:black;
        }
        .form-group input {
            width: 100%;
            padding: 0.5rem;
            border: 1px solid #b2dfb2;
            border-radius: 4px;
        }
        .login-btn {
            width: 100%;
            padding: 0.6rem;
            background: #28a745;
            color: #fff;
            border: none;
            border-radius: 4px;
            font-size: 1rem;
            cursor: pointer;
            margin-top: 0.5rem;
            transition: background 0.2s;
        }
        .login-btn:hover {
            background: #218838;
        }
        .register-link {
            display: block;
            text-align: center;
            margin-top: 1rem;
            color: #28a745;
            text-decoration: none;
            transition: color 0.2s;
        }
        .register-link:hover {
            text-decoration: underline;
            color: #218838;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <h2>Supplier Login</h2>
        <form action="/supplier/login" method="POST">
            <div class="form-group">
                <label for="loginId">Login ID</label>
                <input type="text" id="loginId" name="loginId" required>
            </div>
            <div class="form-group">
                <label for="password">Password</label>
                <input type="password" id="password" name="password" required>
            </div>
            <button class="login-btn" type="submit">Login</button>
        </form>
        <a class="register-link" href="/supplier/register">Don't have an account? Register here</a>
    </div>
</body>
</html>
